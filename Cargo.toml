[workspace]
resolver = "2"
members = ["proxy", "installer"]

# workflow를 위한 메타 데이터
# .github/workflows/release.yml이 이 version을 읽어 tag로 사용.
[workspace.package]
version = "0.1.1"
edition = "2024"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
